# Cloudflare Pages SPA fallback — client-side routes (/draft, /teams, ...) must
# serve index.html rather than 404. Data files are real paths and are matched
# first, so they are unaffected.
/*    /index.html   200
